#include <iostream>
using namespace std;
int main()
{ int a,b;
  cout<<"enter the no."<<endl;
  cin>>a;
  if(a>0)
   cout<<"the absolute no. is"<<a;
  else
   a=-1*a;
  cout<<"the absolute is "<<a;
}
